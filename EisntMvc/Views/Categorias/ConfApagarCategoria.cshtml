@{
    ViewData["Title"] = "Apagar Confirmação";
}

<div class="text-center">
    <h1 class="display-4">Apagar Categoria</h1>
    <p>Deseja realmente apagar a categoria <strong>@Model.Nome</strong>?</p>
</div>
<br/>
<div class="text-center">
    <a asp-controller="Categorias" asp-action="Index" class="btn btn-dark">
        ← Voltar
    </a>

    <!-- Formulário para remover o produto -->
    <form method="post" asp-controller="Categorias" asp-action="ApagarCategoria" asp-route-id="@Model.Id" class="d-inline">
        <button type="submit" class="btn btn-danger">Apagar</button>
    </form>
</div>
<br/>